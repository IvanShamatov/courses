<p id="notice"><%= notice %></p>

<h1>Listing Courses</h1>

<div class="list">
    <% @courses.each do |course| %>
  <strong><%= link_to course.title, course, { :class => "list-group-item" } %></strong>
    <% end %>

</div>

<br>
<nav>
  <ul class="pagination">
    <li><%= link_to 'New Course', new_course_path %></li>
  </ul>
</nav>
